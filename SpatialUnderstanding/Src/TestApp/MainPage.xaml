<Page
    x:Class="TestApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Margin="50,50,50,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" HorizontalAlignment="Center" Click="Button_Click">Click Me To Test</Button>
        <TextBlock Name="PassFailText" Grid.Row="1" HorizontalAlignment="Center" Foreground="Green" Margin="10">Succeeded</TextBlock>
        <TextBlock Name="ResultText" Grid.Row="2" TextWrapping="Wrap">Test results...</TextBlock>
    </Grid>
</Page>
